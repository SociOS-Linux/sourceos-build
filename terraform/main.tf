# SourceOS build substrate — Terraform skeleton (no backend/credentials committed).
terraform {
  required_version = ">= 1.5.0"
}

# Providers and backend are configured per-environment out-of-band.
# Example resource placeholders (build namespace, artifact bucket) go here.
